domain billing {
  owner: "billing-platform"

  projection BillingCustomer @ 1
    from customer.Customer @ 3 as c
  {
    billingId <- c.customerId
    name <- c.fullName
    @pii
    invoiceEmail <- c.email
    isActive = c.status == "active"
  }
}
